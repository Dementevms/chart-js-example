import HorizontalBar from "./applications/horizontal-bar";
import LineChart from "./applications/line-chart";

const config = [
  { id: "root", app: LineChart },
  { id: "root-2", app: HorizontalBar },
];

export default config;
